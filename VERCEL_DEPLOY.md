# دليل النشر على Vercel 🚀

## خطوات النشر

### 1. تثبيت Vercel CLI (اختياري)
```bash
npm install -g vercel
```

### 2. النشر عبر Vercel CLI
```bash
vercel
```

أو النشر مباشرة:
```bash
vercel --prod
```

### 3. النشر عبر GitHub (موصى به)

1. ارفع المشروع إلى GitHub
2. اذهب إلى [vercel.com](https://vercel.com)
3. سجل دخول بحساب GitHub
4. اضغط "New Project"
5. اختر المستودع الخاص بك
6. Vercel سيكتشف الإعدادات تلقائياً
7. اضغط "Deploy"

## تحديث إعدادات ESP32

بعد النشر، ستحصل على رابط مثل:
```
https://your-project.vercel.app
```

قم بتحديث كود ESP32:
```cpp
const char* serverURL = "https://your-project.vercel.app/api/data";
```

## ملاحظات مهمة

⚠️ **تحذير**: Vercel يستخدم serverless functions، مما يعني:
- البيانات المخزنة في الذاكرة (`sensorData`) قد تُفقد عند عدم الاستخدام
- للحل الدائم، استخدم قاعدة بيانات (MongoDB, PostgreSQL, إلخ)

## حل مشكلة فقدان البيانات

للحفاظ على البيانات بشكل دائم، يمكنك:
1. استخدام Vercel KV (Key-Value store)
2. استخدام MongoDB Atlas
3. استخدام Supabase
4. استخدام أي قاعدة بيانات خارجية

## متغيرات البيئة (Environment Variables)

إذا كنت تستخدم قاعدة بيانات، أضف المتغيرات في:
- Vercel Dashboard → Settings → Environment Variables

## اختبار محلي مع Vercel

```bash
vercel dev
```

سيشغل المشروع محلياً مع نفس البيئة التي في Vercel.

