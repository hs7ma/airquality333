# نظام مراقبة العوامل البيئية 🌍

نظام متكامل لمراقبة العوامل البيئية باستخدام ESP32 والحساسات المختلفة مع تطبيق ويب لعرض البيانات.

## المكونات المستخدمة

- **ESP32**: لوحة التحكم الرئيسية
- **DHT22**: حساس درجة الحرارة والرطوبة
- **MQ-2**: حساس الغاز
- **BH1750**: حساس الإضاءة

## الميزات

- ✅ قراءة مباشرة من الحساسات
- ✅ إرسال البيانات عبر WiFi
- ✅ عرض البيانات في الوقت الفعلي
- ✅ واجهة مستخدم جميلة وسهلة الاستخدام
- ✅ تحديث تلقائي كل 5 ثواني

## التثبيت والتشغيل

### 1. تثبيت المكتبات المطلوبة

#### في Arduino IDE:
- DHT sensor library
- BH1750 library
- WiFi library (مدمج في ESP32)

### 2. إعداد كود ESP32

1. افتح ملف `esp32_code/esp32_sensors.ino` في Arduino IDE
2. قم بتعديل إعدادات WiFi:
   ```cpp
   const char* ssid = "YOUR_WIFI_SSID";
   const char* password = "YOUR_WIFI_PASSWORD";
   ```
3. قم بتعديل عنوان السيرفر:
   ```cpp
   const char* serverURL = "http://YOUR_SERVER_IP:3000/api/data";
   ```
   (استبدل `YOUR_SERVER_IP` بعنوان IP لجهاز الكمبيوتر الذي يعمل عليه السيرفر)

### 3. توصيل الحساسات

#### DHT22:
- VCC → 3.3V
- GND → GND
- DATA → Pin 4

#### MQ-2:
- VCC → 5V
- GND → GND
- A0 → Pin 34 (Analog)

#### BH1750:
- VCC → 3.3V
- GND → GND
- SDA → GPIO 21
- SCL → GPIO 22

### 4. تشغيل السيرفر

1. قم بتثبيت Node.js على جهازك
2. افتح Terminal في مجلد المشروع
3. قم بتثبيت المكتبات:
   ```bash
   npm install
   ```
4. شغل السيرفر:
   ```bash
   npm start
   ```
   أو للتطوير مع إعادة التشغيل التلقائي:
   ```bash
   npm run dev
   ```

### 5. فتح التطبيق

افتح المتصفح وانتقل إلى:
```
http://localhost:3000
```

## هيكل المشروع

```
invironmenetapp/
├── esp32_code/
│   └── esp32_sensors.ino    # كود ESP32
├── server/
│   └── server.js            # سيرفر Node.js
├── public/
│   ├── index.html           # صفحة الويب الرئيسية
│   ├── style.css            # ملف التنسيق
│   └── app.js               # كود JavaScript
├── package.json             # إعدادات Node.js
└── README.md               # ملف التوثيق
```

## API Endpoints

### POST /api/data
استقبال البيانات من ESP32
```json
{
  "temperature": 25.5,
  "humidity": 60.0,
  "gas": 300,
  "light": 450.5
}
```

### GET /api/data
الحصول على آخر قراءة من الحساسات

## ملاحظات مهمة

1. تأكد من أن ESP32 و الكمبيوتر متصلان على نفس شبكة WiFi
2. تأكد من تعطيل جدار الحماية أو إضافة استثناء للبورت 3000
3. يمكنك تعديل معدل التحديث في كود ESP32 (`timerDelay`) و JavaScript (`UPDATE_INTERVAL`)

## النشر على Vercel 🚀

المشروع جاهز للنشر على Vercel! راجع ملف `VERCEL_DEPLOY.md` للتفاصيل الكاملة.

### خطوات سريعة:

1. **تثبيت Vercel CLI** (اختياري):
   ```bash
   npm install -g vercel
   ```

2. **النشر**:
   ```bash
   vercel
   ```

3. **تحديث ESP32**:
   بعد النشر، ستحصل على رابط مثل `https://your-project.vercel.app`
   قم بتحديث كود ESP32:
   ```cpp
   const char* serverURL = "https://your-project.vercel.app/api/data";
   ```

⚠️ **ملاحظة مهمة**: Vercel يستخدم serverless functions، البيانات المخزنة في الذاكرة قد تُفقد. للحل الدائم، استخدم قاعدة بيانات.

## التطوير المستقبلي

- [ ] إضافة قاعدة بيانات لحفظ تاريخ القراءات
- [ ] إضافة رسوم بيانية لتتبع البيانات
- [ ] إضافة تنبيهات عند تجاوز القيم الآمنة
- [ ] إضافة دعم لعدة أجهزة ESP32
- [ ] إضافة نظام مصادقة

## الرخصة

MIT License

